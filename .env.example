# MCP Resource Server Configuration
#
# This file is OPTIONAL. The server works with sensible defaults.
# Copy this file to '.env' and uncomment/modify only what you need.
#
# All variables shown here are optional and have default values.

# File Source Configuration
# Pattern for constructing file URLs. {file_id} will be replaced.
# Examples:
#   Local filesystem (default): file:///mnt/resources/{file_id}
#   Custom API: https://api.example.com/files/{file_id}
#   Network share: file:///mnt/network-storage/{file_id}
#   Cloud storage: https://mybucket.s3.amazonaws.com/{file_id}
# RESOURCE_SERVER_URL_PATTERN=file:///mnt/resources/{file_id}

# MCP Server Configuration (optional)
# RESOURCE_SERVER_DEBUG=true
# RESOURCE_SERVER_MASK_ERRORS=false

# Blob Storage Configuration (optional)
# Used by get_image_resource() and get_file_resource() for shared file storage
# RESOURCE_SERVER_BLOB_STORAGE_ROOT=/mnt/blob-storage
# RESOURCE_SERVER_BLOB_STORAGE_MAX_SIZE_MB=100
# RESOURCE_SERVER_BLOB_STORAGE_TTL_HOURS=24
